import React from 'react';
import './Footer.css';

export default function Footer(): React.ReactElement {
    return (
        <footer>
            <p>Made with 🧡 and React</p>
        </footer>
    );
}
